extends layout.pug

block content
    h1 Book Details
        p
            a.button(href='/new_book') Create New Book
        table
            thead
                tr
                    th
                    th
                    th
                    th
            tbody
                tr
                    each book in books                
                        td
                            a(href=`/books/${book.title}`)
                    td= book.author 
                    td= book.genre
                    td= book.year